import json
with open("students.json", "r", encoding="utf-8") as file:
    students = json.load(file)
for s in students:
    grades = s["grades"]
    s["AVG"] = round(sum(grades) / len(grades), 2)
with open("AVG.json", "w", encoding="utf-8") as file:
    json.dump(students, file, indent=4)
