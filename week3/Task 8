#8.1
n = int(input())
print(n)
for num in range(1, n + 1):
    s = str(num)
    A = True
    for i in s:
        d = int(i)
        if d == 0 or num % d != 0:
            A = False
            break
    if A:
        print(num, end=" ")

#8.2
def swap(arr):
    if len(arr) > 1:
        arr[0], arr[-1] = arr[-1], arr[0]
m = int(input())
print(m)
A = input().split()
A = [int(x) for x in A[:m]]
print(A)
swap(A)
print(A)
